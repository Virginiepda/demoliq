{% extends 'base.html.twig' %}

 {% block title %}DemoLiquide | Les questions à débattre{% endblock %}

{% block content %}
    <h1>Les questions à débattre</h1>

    <table class="table table-striped">
        <tr>
            <th>Question</th>
            <th>Appuis</th>
            <th>Date</th>
            <th>Actions</th>
        </tr>
            {% for question in questions %}
        <tr>
        <td>{{ question.title }}</td>
            <td>{{ question.supports }}</td>
            <td>{{ question.creationDate|date('d-m-Y H:i:s') }}</td>
            <td><a class="btn btn-secondary" href="#">Débattre</a></td>
        </tr>
        {% endfor %}
    </table>

{% endblock %}
